# RestFramework
This project is a basic e-commerce website built using Django, along with a REST API that categorizes movies based on user input from an HTML form. The API processes movie data into three different categories, such as Romantic, Action, and others.
